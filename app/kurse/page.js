import KurseClient from './KurseClient';

export const metadata = {
  title: 'Kurse',
  description: 'Entdecke unsere Pilates-Kurse: Reformer, Barre, Mat, Pre & Postnatal und Move & Grow. Boutique Pilates in kleinen Gruppen in Lichterfelde West, Berlin.',
};

export default function KursePage() {
  return <KurseClient />;
}
